# M2 执行清单（索引）

## 权威来源

本任务的执行清单以审计任务已审核版本为唯一权威，不在此重复维护：

- **完整执行清单**：`.trellis/tasks/08-10-m2-readiness-audit/implement.md`
  - 执行原则（第 1 节）
  - 预检与基线：工作区保护、素材引用预检、代码搜索（第 2 节）
  - 里程碑一：静态 UGUI 线框（第三版，第 3 节）——审核门槛 A：用户批准第三版线框（第二版不作数）
  - 里程碑二：运行时组件骨架（第 4 节）
  - 里程碑三：四阶段流程（第 5 节）
  - 里程碑四：透视、帮助、重置与完成出口（第 6 节）
  - 里程碑五：全身数字人与问答复用（第 7 节）——审核门槛 B：核心流程 + M1 QA/数字人回归
  - 里程碑六：M1→M2 串联（第 8 节）
  - 全量质量检查（第 9 节）与回滚点 A~D（第 10 节）
  - 文件边界（第 11 节）与最终审核提交门槛（第 12 节）

## 2026-08-12 最终收口状态

- 正式尺子已由子任务 `08-12-m2-final-functional-closeout` 接入，改为全程可见、初态置灰锁定、Step 4 解锁和零刻度吸附；本文件下方“初始隐藏”是 2026-08-11 的历史方案，已被覆盖。
- 完整 M2 QA/数字人引用、重置暂停、110mm 视觉校准、探头/声束角度反馈和 Build Settings 已完成。
- Editor Play Mode 自动验收已通过 M2 核心四阶段与 M1→M2 实际加载；M2 最终冻结 SHA-256 为 `3ef75ced51304258b5bde9b43be8f354b247753801a708ae52b922b5829c990b`。
- 剩余仅为主编辑器人工体验检查：数字人视频画质/三态、长按手感、30/60秒等待和配置 API Key 后的真实网络回复。

## 2026-08-11 同步要点

- 里程碑一改为**第三版静态线框**：全幅主场景 + 紧凑 B 区（约 460x240，低对比、不纵向贯穿）+ 右侧 DigitalHumanStage 舞台/构图（约 300–320px 无边框）；静态审核使用可识别的全身预览或 Play Mode 运行截图，不只放头像；第二版线框不作为门槛 A 通过依据。
- **运行时组件 4→5 拆分说明（审核后确认）**：防卡死帮助计时独立为 `M2IdleHelp`（30/60 秒提示 + 自动演示编排）。理由：① FlowController 作为唯一状态所有者已近 150 行上限，继续承担计时/演示会超标；② 帮助是只读阶段计时的独立职责，拆出后 Flow 只保留状态迁移与 UI 协调；③ M2IdleHelp 通过 `flow.CurrentStage` 只读消费，不破坏"Flow 是唯一状态所有者"契约；④ 拆后 Flow 回到可维护规模。审核确认此拆分合理，文档同步为 5 个 runtime 组件。
- **Step 4 尺子激活修正（历史，已覆盖）**：当时方案为尺子初始隐藏；最终产品合同已改为全程可见、前期置灰锁定，见上方 2026-08-12 状态。
- **扫描区坐标合同统一**：`scanStartLocal`=0.143 / `scanEndLocal`=0.857（钢轨 891px 居中于 viewport），拖动计算与自动演示同一映射，不再分 0.10→0.90 与 0.05→0.95。
- **步骤提示文案配置化**：`M2FlowController.stepHints`（string[]）移入 Inspector，运行时不再硬编码。
- 里程碑五更名「全身数字人与问答复用」：静态阶段只预留舞台/构图，实际视频/Presenter/QA 在核心流程独立验收后接入。
- 数字人/问答复用 M1 链路（M1DigitalHumanPresenter / M1PressDetector / M1QAPanel / M1DeepSeekClient / UI-LumaKey-DigitalHuman 与三个 MP4），不新增数字人 runtime 脚本；Editor 侧提取参数化公共 Ensure，不复制 M1QASetup 整段逻辑；M1 行为回归必须检查。
- 其余保留：无返回 M1、无引导、可配置下一模块出口、尺子/波形/交互合同不变。

## 执行顺序速查

1. 预检（git status / 素材 / 代码搜索）→ 里程碑一（M2Setup 第三版静态线框，含数字人舞台/构图占位）→ **用户审核三视口截图**（第二版不作数）→ 里程碑二（4 个 runtime 组件 + Setup 注入）→ 里程碑三（四阶段流程）→ 里程碑四（透视/帮助/重置/出口）→ **独立验收** → 里程碑五（全身数字人与问答复用，含 M1 回归保护）→ **审核门槛 B** → 里程碑六（M1→M2 串联，需用户确认）→ 全量质量检查。

每个里程碑完成后先跑最小相关检查，再进入下一阶段；最终审核前不执行 `git commit` / `git push`。
